syntax = "proto3";

package litepb;

option go_package = "github.com/e-tape/litepb/proto;litepb";

import "options.proto";

// Implementation of uuid.UUID transfer
message UUID {
  // Value of UUID
  bytes value = 1 [(litepb.array_fixed_size) = 16];
}